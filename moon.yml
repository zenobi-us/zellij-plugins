---
$schema: "file://./.moon/cache/schemas/project.json"

id: repo
language: rust
layer: tool

tasks:
  build:
    deps:
      - "zellij-tabbar:build"
    command: "noop"

  check:
    deps:
      - "zellij-tabbar:check"
    command: "noop"

  test:
    deps:
      - "zellij-tabbar:test"
    command: "noop"

  e2e:
    deps:
      - "zellij-tabbar:e2e"
    command: "noop"

  install:
    deps:
      - "zellij-tabbar:install"
    command: "noop"
    options:
      runInCI: false
